const BASE = 'https://xkecqmsgvjjtujvlotpm.supabase.co';
const SVC  = 'eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InhrZWNxbXNndmpqdHVqdmxvdHBtIiwicm9sZSI6InNlcnZpY2Vfcm9sZSIsImlhdCI6MTc2NjU4NDIxNSwiZXhwIjoyMDgyMTYwMjE1fQ.jOOgWSgXTqOKtr1fFqh4RIhwXzd7ZcF9XjOihpCHCnU';
const H    = { 'apikey': SVC, 'Authorization': 'Bearer '+SVC, 'Content-Type': 'application/json' };

async function appendAndPatch(id, addition) {
  const r = await fetch(BASE+'/rest/v1/lessons?id=eq.'+id+'&select=content', { headers: H });
  const [row] = await r.json();
  const newContent = row.content + addition;
  const w = newContent.split(/\s+/).filter(Boolean).length;
  const p = await fetch(BASE+'/rest/v1/lessons?id=eq.'+id, {
    method: 'PATCH', headers: { ...H, 'Prefer': 'return=minimal' },
    body: JSON.stringify({ content: newContent })
  });
  console.log(p.ok ? '✅' : '❌ '+p.status, id.slice(0,8), w+' mots');
}

// L7 at 3985, need ~50 more words to clear 4000
await appendAndPatch('c7d769eb-22d9-4d69-bc52-4261b428b47a', `

## Note finale sur le choix de la formation

Le choix d'une formation dans le système LMD est une décision qui engage plusieurs années de vie académique et professionnelle. Prendre le temps de bien explorer les options, de consulter les personnes qui exercent les métiers visés, et de s'informer sur les débouchés réels de chaque formation est le meilleur investissement qu'un candidat puisse faire avant de s'engager dans un cursus. La variété et la richesse du système français offrent une palette de possibilités exceptionnelle pour construire un projet de formation sur mesure.
`);
